# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.next
next-env.d.ts

# raw hero animation source frames (132MB) — processed output lives in
# public/hero-frames/ and is what the site actually uses. Keep the source
# frames locally if you want to reprocess with different keying settings.
/public/hero/
.env
.env.*.local
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
